# membrane-globality probes — 2026-09-05/06 (фаза 0 роя «единственное горло»)

База: `master` `a60de1c50`. Прогон: macOS, Node 24.18.1, резолюция src
(`cd benchmarks && NODE_OPTIONS='--conditions=@real-router/internal-source' npx tsx <файл>`).
Проверено после переноса каталога: пробы запускаются с этого места, относительные импорты
к `packages/core/tests/helpers/hostileBags` рассчитаны на глубину `<эта дира>/<семейство>/`.
Абсолютные пути на worktree, в котором шёл рой, вычищены (67 вхождений в 10 файлах);
`_regroup/check.mjs` переведён на `import.meta.url` и запускается из любого каталога.

⚠ Изменения в `master` после `a60de1c50` (`90a70cce9`) затронули `port.ts` и
`StateNamespace.ts` **только комментариями** — вердикты ниже не устарели.

Вопрос фазы 0: сколько дверей обязаны держать ручку на объект вызывающего вместо копии
контейнера на границе. Промпт и отчёт — `packages/core/.claude/`
(`swarm-membrane-globality-2026-09-05.md`, `membrane-globality-phase0-report-2026-09-06.md`).

## Итог

Знаменатель — **216 дверей** (249 строк переписи − 33 хэндаута без round-trip и не-двери).

```
MUST-(б)        23 сырых → 3 механизма контейнеров ПРИЛОЖЕНИЯ (остальное: 14 путей —
                объекты ядра, отданные ручкой; 15 — листы и протокольные объекты)
CANNOT-AFFORD    9 путей → 4 механизма, все на ВЫДАЧЕ
COULD-(а)      159 → 139 (65 уже (а) сегодня, 74 ручка)
```

**H держится:** копия контейнера на границе применима как умолчание. Три исключения держатся
пинами (#1958 алиасинг дефолтов, #1796 перечитывание `queryParams` клоном, #1832/#1957
идентичность вложенных мешков опций), а не природой объекта.

Цена (а) там, где она реальна: PENDING-шелл +11…13 % на навигацию (пол A/A 2,6 %); кэш
`shouldUpdateNode` +683 % (199 нс → 1,56 мкс, пол 3,3 %); запись `getRouteConfig` +37 %
(16,6 → 22,8 нс, пол −0,1 %); гейт popstate +15 % (75 → 87 нс, пол −1,4 %).

Главная механическая находка: горла копируют один раз, но **перед** горлом стоят от одного
до трёх читателей того же контейнера (валидаторы `validation-plugin` — 32 двери,
гварды каналов, `guardRouteStructure`). P1 нарушено на 78 дверях из 216.

## Что где

| каталог | что внутри |
| --- | --- |
| `_census/` | `census.json` — перепись дверей; `critic.json` — 13 находок критика полноты; `phase0b-result.json` — вердикты, числа, триаж, исключённые хэндауты; `seed-control-0.json` — контроль пятью семенами (5/5) |
| `cls-<семейство>/` | классификация: `verdict.json` (полный вердикт по каждой двери — механизм, пробы, решающие строки, цена, P1–P4) + пробы-матрицы; `verify-1.json` там, где проверяющий успел |
| `control-control-{fsm,logger}/` | контрольная рука: `fsm` 10 дверей / 4 MUST-(б) / 0 P-нарушений, `logger` 7 / 0 / 3 |
| `_regroup/` | группировка семейств + `check.mjs` (покрытие 216 уникальных, дублей нет) |
| `_env/probe-env.ts` | позитивный контроль среды: ядро резолвится в src, фикстуры импортируются, счётчик чтений работает |
| остальные | пробы первой волны (перепись по 8 линзам, дополнения критика) |

## Отсеяно перед коммитом

`_census/sweeps-all.json` и `merge-light-*.json` — сырой вывод восьми линз и группировки
слияния: провенанс, вытесненный `census.json` (в нём у каждой двери `rawKeys` и `lensesSeen`).
`census-190.json` — промежуточная сборка с дефектом перестановки (202 двери, 36 неразобранных),
опасная рядом с верной. `cls-dependencies-store-naruzhu/` — брошенный дубль каталога, ни один
вердикт на него не ссылается.

## Как перечитать вердикт

```bash
node -e 'const c=require("./_census/census.json");
  console.log(c.doors.filter(d=>d.family==="params·normalizeChannel"))'
```

Полный вердикт по семейству — в его `cls-*/verdict.json`; поле `proof.decisiveOutput` несёт
дословные строки вывода пробы, `proof.positiveControl` — чем доказано, что проба дошла до ветки.
