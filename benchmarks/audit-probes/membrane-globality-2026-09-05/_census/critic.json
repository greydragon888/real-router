{
 "missing": [
  {
   "what": "Семейство «валидатор-колбэки·параметры» недосчитано: перепись держит 5 дверей, а колбэков RouterValidator, получающих КОНТЕЙНЕР ВЫЗЫВАЮЩЕГО по ссылке до/рядом с копией ядра (тот же критерий, что у censused validateDependenciesObject·deps), — 26 (+1 лист, +5 снапшот-с-вложенными). Новые id: RouterValidator.routes.validateIsActiveRouteArgs·params; RouterValidator.navigation.validateSearch·search ×7 (isActiveRoute, buildPath, canNavigateTo, navigate — обе формы, PluginApi.makeState, PluginApi.forwardState, PluginApi.buildNavigationState); RouterValidator.navigation.validateParams·params ×3 (buildPath, canNavigateTo, navigate); RouterValidator.routes.validateStateBuilderArgs·params ×4 (PluginApi.forwardState, PluginApi.buildNavigationState, navigate через wireNamespaces · buildNavigateState — ВТОРОЙ читатель того же P, и RoutesNamespace.matchPath — decoded.params, т.е. возврат decodeParams); RouterValidator.navigation.validateNavigationOptions·options ×3 (navigate, navigateToDefault, PluginApi.navigateToState); RouterValidator.navigation.validateNavigateToDefaultArgs·options; RouterValidator.navigation.validateNavigateToStateArgs·state; RouterValidator.state.validateMakeStateArgs·params; RouterValidator.state.validateAreStatesEqualArgs·s1|s2; RouterValidator.routes.validateUpdateRouteBasicArgs·updates + validateUpdateRoutePropertyTypes·updates + validateUpdateRoute·updates — ТРИ прохода по одному патчу; RouterValidator.plugins.validatePluginKeys·plugin (объект плагина до freeze); лист: RouterValidator.dependencies.validateSetDependencyArgs·value; снапшот-варианты (контейнер — снапшот ядра, но вложенные defaultParams/defaultSearch вызывающего по ссылке): validateRoutes·batch, validateAddRouteArgs·batch, throwIfInternalRouteInArray·batch (add и replace), guardRouteCallbacks·route, guardNoAsyncCallbacks·route.",
   "where": "packages/core/src/Router.ts · isActiveRoute / buildPath / canNavigateTo / navigate / navigateToDefault / areStatesEqual; packages/core/src/api/getPluginApi.ts · makeState / forwardState / navigateToState / buildNavigationState; packages/core/src/api/getRoutesApi.ts · api.update / api.add / api.replace; packages/core/src/wiring/wireNamespaces.ts · buildNavigateState; packages/core/src/namespaces/RoutesNamespace/RoutesNamespace.ts · matchPath; packages/core/src/namespaces/PluginsNamespace/PluginsNamespace.ts · #startPlugin; packages/core/src/api/getDependenciesApi.ts · set; packages/core/src/guards.ts · guardRouteCallbacks",
   "evidence": "benchmarks/audit-probes/membrane-globality-2026-09-05/census-completeness/probe-validator-identity.ts — записывающий валидатор ставится в getInternals(router).validator (тот же слот, что у validation-plugin), каждая дверь вызывается с маркер-объектом. Решающие строки: recordedWithoutValidator: 0 (негативный контроль); для каждой перечисленной двери same: true, count: 1; door «Router.navigate·routeParams (2nd reader)» method routes.validateStateBuilderArgs same: true; door «RoutesApi.update·updates (3rd reader)» method routes.validateUpdateRoute same: true; door «RoutesApi.add·routes[]»: batchIsCallerArray: false, batchElementIsCallerRoute: false, nestedDefaultParamsIsCallers: true; guardRouteCallbacks·route: isCallerRoute: false, nestedDefaultParamsIsCallers: true; censused-контроли validateDependenciesObject / validateCloneArgs / validateNoDuplicatePlugins тоже same: true. Полнота перечисления call-sites: обе формы — ctx.validator?.<group>.<method> (63 попадания по git ls-files packages/core/src) и getValidator()?. / const validator = …getValidator() (6 + 3 сайта: PluginsNamespace, RouteLifecycleNamespace, EventBusNamespace, RoutesNamespace) — остальные сайты принимают строки/числа/функции. Одноимённость символов доказана записью точного имени метода в calls[].m."
  },
  {
   "what": "PENDING-шелл (plan.toState) раздаётся наружу и на арках CANCEL / ERROR / SAME_STATES, а семейство «PENDING-State-шелл-наружу·ручка» перечисляет только start / leaveApprove / leaveState.nextRoute. Новые id: Plugin.onTransitionCancel·toState, Plugin.onTransitionCancel·toState.context, PluginApi.addEventListener·$$cancel·toState, Plugin.onTransitionError·toState (арка in-band FAIL — inflight.toState; и арка SAME_STATES — шелл materializePending, никогда не анонсированный через onTransitionStart), Plugin.onTransitionError·toState.context, PluginApi.addEventListener·$$error·toState. Объект — тот же незамороженный шелл, что получил onTransitionStart; записи в него ложатся.",
   "where": "packages/core/src/namespaces/EventBusNamespace/EventBusNamespace.ts · #setupFSMActions → handleCancel (emitTransitionCancel(inflight.toState, fromState)) и emitNavigationFail (emitTransitionError(inflight?.toState, payload.fromState, error)); packages/core/src/namespaces/NavigationNamespace/transition/executeNavigation.ts · executeNavigation (ветка isSameNavigation → deps.emitTransitionError(toState, fromState, CACHED_SAME_STATES_ERROR))",
   "evidence": "census-completeness/probe-pending-shell-cancel-error.ts: onTransitionCancel_toState {name: b, frozenShell: false, frozenParams: true, frozenContext: false}; onTransitionCancel_isStartedShell: true; onTransitionCancel_writesLanded: true; $$cancel_sameAsHook: true; cancelOutcome: CANCELLED; onTransitionError_CANNOT_ACTIVATE_toState {frozenShell: false}; onTransitionError_isStartedShell: true; $$error_sameAsHook: true; onTransitionError_SAME_STATES_toState {name: c, frozenShell: false}; sameStatesOutcome: SAME_STATES; позитивный контроль committed {frozenShell: true}."
  },
  {
   "what": "Вложенное поле RouterInternals.systemCommit·toState.transition.segments (и симметрично Router.getState·return.transition.segments / хуки / subscribe на этой арке): adoptForeignBag копирует и морозит ОДИН уровень transition, а контейнер segments приложения остаётся в закоммиченном состоянии по ссылке, незамороженный, и раздаётся обратно через каждый .transition-хэндаут — ядро публикует как свой уровень, которого не заморозило (P4 в обратную сторону). Докблок systemCommit («Its nested segments is frozen by buildTransitionMeta») верен только для transition, построенного ядром.",
   "where": "packages/core/src/namespaces/EventBusNamespace/EventBusNamespace.ts · systemCommit (transition: adoptForeignBag(foreignTransition, EMPTY_PARAMS)); packages/core/src/helpers.ts · adoptForeignBag; читатели segments вне ядра — docblocks useRouteEnter в packages/react|preact|solid (route.transition.segments.activated.includes(...))",
   "evidence": "census-completeness/probe-systemcommit-segments.ts: after {committedIsGetState: true, transitionIsAppObject: false, transitionFrozen: true, segmentsIsAppObject: true, segmentsFrozen: false, activatedFrozen: false}; после записи через удержанную ручку: activatedSeenThroughGetState: [a, injected-after-commit], intersectionSeenThroughGetState: rewritten; контроль (обычный navigate): control {frozenTransition: true, frozenSegments: true, frozenActivated: true}."
  },
  {
   "what": "NavigationOptions.signal — объект приложения (тип AbortSignal, но принимается любая утка/Proxy), который ядро ДЕРЖИТ на всё время навигации и читает в нескольких кадрах: aborted на входе, при adopt после анонса, в mayCommit; addEventListener в мосте; removeEventListener на COMPLETE/CANCEL/FAIL; reason при аборте. Ни одной *·options.signal двери в переписи. Новые id: Router.navigate·options.signal, Router.navigateToDefault·options.signal, PluginApi.navigateToState·options.signal, RouterInternals.navigateToState·options.signal, navigateWithHash·extraOptions.signal (shared/dom-utils — spread в opts). Стратегия (б) по необходимости (лист-идентичность), но это отдельная дверь с собственным P1-профилем (aborted ×4).",
   "where": "packages/core/src/namespaces/NavigationNamespace/transition/executeNavigation.ts · executeNavigation (const externalSignal = opts.signal; abortedAtEntry; abortedSignal?.aborted после beginTransition); packages/core/src/routerFSM.ts · mayCommit (payload.externalSignal?.aborted !== true); packages/core/src/namespaces/EventBusNamespace/EventBusNamespace.ts · bridgeExternalSignal / bridgeSignal (signal.addEventListener(abort, …), signal.removeEventListener, signal.reason); shared/dom-utils/link-utils.ts · navigateWithHash",
   "evidence": "census-completeness/probe-signal-handle.ts (утка с счётчиками): live {aborted: 4, reason: 0, addEventListener: 1, removeEventListener: 1, committed: b}; abortedMidFlight (abort через удержанную ручку из парковки guard) {aborted: 3, reason: 1, addEventListener: 1, removeEventListener: 1, outcome: CANCELLED, stillAt: a}; негативный контроль без слушателей noBridge {aborted: 4, addEventListener: 0, removeEventListener: 0} — чтения aborted идут и без моста."
  },
  {
   "what": "Семейство «thenable-returns» держит 3 двери из ≥10: EventEmitter#invokeIsolated опрашивает .then у возврата КАЖДОГО слушателя и при функции отдаёт объект в Promise.resolve — потребляются возвраты всех 7 хуков Plugin, колбэков addEventListener (PluginApi и RouterInternals) и слушателя subscribe. Новые id: Plugin.onStart·return, Plugin.onStop·return, Plugin.onTransitionStart·return, Plugin.onTransitionLeaveApprove·return, Plugin.onTransitionCancel·return, Plugin.onTransitionError·return, Plugin.onTransitionSuccess·return, PluginApi.addEventListener·cb·return, RouterInternals.addEventListener·cb·return, Router.subscribe·SubscribeFn·return. НЕ дверь: RoutesApi.subscribeChanges·handler·return — обёртка subscribeTreeChanged отбрасывает возврат.",
   "where": "packages/core/src/utils/event-emitter/EventEmitter.ts · #invokeIsolated (typeof result.then === function → Promise.resolve(result).catch(...)); packages/core/src/namespaces/EventBusNamespace/EventBusNamespace.ts · subscribe (возвращает result слушателя в эмиттер, #944/#1412) и · subscribeTreeChanged ((event) => { handler(event); } — возврат отброшен)",
   "evidence": "census-completeness/probe-thenable-returns-emitter.ts: Plugin.onStart·return / onTransitionStart·return / onTransitionLeaveApprove·return / onTransitionSuccess·return / PluginApi.addEventListener·cb·return / Router.subscribe·SubscribeFn·return — все {thenReads: 2, thenCalls: 1}; RoutesApi.subscribeChanges·handler·return {thenReads: 0, thenCalls: 0}; plainControl {thenReads: 1, thenCalls: 0} — любой объектный возврат опрашивается на then. Хуки onStop / onTransitionCancel / onTransitionError отдельно не прогонялись — тот же единственный диспетчер #emitter.emit → #invokeIsolated, перечислены по символу."
  },
  {
   "what": "Хэндаут инстанса Router фабрикам приложения: перепись держит только PluginFactory·router, а тот же объект получают все guard-фабрики и loader-фабрики shared/ssr. Новые id: Route.canActivate·GuardFnFactory·router, Route.canDeactivate·GuardFnFactory·router, LifecycleApi.addActivateGuard·GuardFnFactory·router, LifecycleApi.addDeactivateGuard·GuardFnFactory·router, RoutesApi.update·updates.canActivate|canDeactivate·GuardFnFactory·router, SsrLoaderFnFactory·router (shared/ssr). Сама перепись в описании Router·[key: string] пишет «измерено: … GuardFnFactory·router получают тот же объект», но id не завела.",
   "where": "packages/core/src/wiring/wireNamespaces.ts · createCompileFactory (factory(router, getDependency)); packages/core/src/namespaces/RouteLifecycleNamespace/RouteLifecycleNamespace.ts · compileGuardFactory / #registerHandler (this.#deps.compileFactory(factory)); shared/ssr/createSsrLoaderPlugin.ts · compile (obj.loader(router, getDependency))",
   "evidence": "census-completeness/probe-router-handout-factories.ts: все 7 записей isRouterInstance: true, arrived: true (Route.canActivate, Route.canDeactivate, LifecycleApi.addActivateGuard, LifecycleApi.addDeactivateGuard, RoutesApi.update·updates.canActivate, censused-контроль PluginFactory·router, SsrLoaderFnFactory·router)."
  },
  {
   "what": "validateRoute·route (+ рекурсивно ·route.children[]) — ПУБЛИЧНЫЙ экспорт @real-router/core/validation, которым validation-plugin обходит объекты маршрутов ВЫЗЫВАЮЩЕГО до снапшота ядра — тот же класс, что censused validateDependenciesObject·deps (второй полный проход по контейнеру); причём не один проход: name ×4, path ×2, encodeParams ×2, decodeParams ×2, children ×3 на каждом уровне. Позиция «параметр», objectKind route-config. (validateRouteType из validation.ts не экспортируется — проверено, TypeError при импорте.)",
   "where": "packages/core/src/validation.ts (export { validateRoute } from ./engine); packages/core/src/engine/validation/route-batch.ts · validateRoute → validateRouteType (getPrototypeOf + hasGettersOrSetters — дескрипторный обход) → validateRouteName / validateRoutePath / validateEncodeParams / validateDecodeParams → рекурсия по routeDef.children",
   "evidence": "census-completeness/probe-validate-route-export.ts (countingProxy-маршрут с ребёнком): routeReads {name: 4, path: 2, encodeParams: 2, decodeParams: 2, children: 3}; childReads {name: 4, path: 2, encodeParams: 1, decodeParams: 1, children: 1}; контроль: маршрут с геттером отвергнут «[router.add] Route must not have getters or setters» — многократные чтения достижимы только Proxy (та же оговорка, что у переписи для #1911)."
  },
  {
   "what": "RouterInternals.routeGetStore().matcher.buildPath·params|search (+ ·options) — ПАРАМЕТРНАЯ дверь через хэндаут живого стора: мешки вызывающего уходят в печатающий движок по идентичности, минуя canonicalize / normalizeChannel / seam. Перепись держит port().buildPath·params|search и четыре matcher.*·return, но не эту.",
   "where": "packages/core/src/engine/path-matcher/SegmentMatcher.ts · buildPath (#buildUrlPath(route, params), печать query из search); достижимо через packages/core/src/Router.ts · registerInternals.routeGetStore → RoutesStore.matcher (опубликовано субпутём @real-router/core/validation)",
   "evidence": "census-completeness/probe-matcher-buildpath-handout.ts: matcherBuildPath {printed: /u/7?tab=x, paramsReads {id: 1}, searchReads {tab: 1}}; driftingSearch {printedDrift: /u/1?tab=first, reads {tab: 1}} — мешок потреблён напрямую; контроль publicControl {publicHref: /u/7?tab=x, same: true}."
  },
  {
   "what": "Вложенные мешки ПРИЛОЖЕНИЯ внутри хэндаутов options: PluginApi.getOptions·return.queryParams|limits|defaultParams|defaultSearch и RouterInternals.getCloneState·return.options.queryParams|limits|defaultParams|defaultSearch — объекты вызывающего по ссылке внутри замороженной записи (freeze одного уровня, #1832), а конструктор клона ПЕРЕЧИТЫВАЕТ queryParams (второе чтение того же объекта приложения через хэндаут; документировано в cloneRouter как намеренное), limits подменяется резолвленным снапшотом (#1880 держится), defaultParams — общая ручка двух роутеров (по одному чтению на navigateToDefault каждого). Перепись перечисляет getCloneState·return.limits/limitKeys/options, но не эти app-owned слоты.",
   "where": "packages/core/src/namespaces/OptionsNamespace/OptionsNamespace.ts · constructor (spread + freeze одного уровня); packages/core/src/Router.ts · registerInternals.getCloneState (options: { ...this.#options.get() }), · deriveMatcherOptions / snapshotQueryParams / asKey; packages/core/src/api/cloneRouter.ts · cloneRouter (new RouterClass(routes, { ...options, … }))",
   "evidence": "census-completeness/probe-clone-rereads-options.ts: identity {getOptions_queryParams_isAppBag: true, getOptions_limits_isAppBag: true, getOptions_defaultParams_isAppBag: true, getCloneState_options_queryParams_isAppBag: true, getCloneState_options_limits_isAppBag: true, getCloneState_limits_isAppBag: false}; queryParams reads после createRouter — по 1 на 4 ключа, после cloneRouter — по 2 (afterClone); limits maxListeners 1 → 1 (не перечитан); defaultParams id: 1 после navigateToDefault базы, 2 после navigateToDefault клона; clone_getOptions_queryParams_isAppBag: true."
  },
  {
   "what": "shared: три связки «объект приложения → дверь ядра» без id: (a) resolveLinkTarget·to — дескриптор <Link to> (NavigationTarget) приложения читается по имени (name/params/search по одному разу), params/search уходят ПО ИДЕНТИЧНОСТИ в buildHref → router.buildPath и navigateWithHash → router.navigate — shared-двойник censused Router.navigate·target; (b) createPluginBuildUrl·opts ({hash}) и (c) createReplaceHistoryState·options ({hash}) — мешки опций вызывающего рядом с censused params|search, hash читается ДВАЖДЫ (opts?.hash === undefined → normalizeHashInput(opts.hash)) — историческая форма P1 (#1899) в shared.",
   "where": "shared/dom-utils/link-utils.ts · resolveLinkTarget; shared/browser-env/plugin-utils.ts · createPluginBuildUrl, · createReplaceHistoryState",
   "evidence": "census-completeness/probe-shared-descriptor-reads.ts: descriptor {readsOfToAfterResolve {name: 1, params: 1, search: 1}, readsOfToAfterBuildHref — те же, resolvedParamsIsAppObject: true, hrefFromCoreDoor: /a/1?q=x}; createPluginBuildUrl_opts {url: /a/1?q=x#sec, reads {hash: 2}}; createReplaceHistoryState_options {reads {hash: 2}}."
  },
  {
   "what": "shared/ssr: createSsrLoaderPlugin·start-interceptor·hydrationState.name|params|search — вложенные поля объекта приложения (object-form hydrateRouter кладёт объект приложения по ссылке в ctx.hydrationState; string-form — возврат deserialize, censused как HydrateRouterOptions.deserialize·return) читаются и ПЕРЕЧИСЛЯЮТСЯ (objectKeys(bag) + bag[key]) в channelAgrees и решают, писать ли claim.write. Перепись держит только .context[namespace] и hydrated[deferredKeysNamespace].",
   "where": "shared/ssr/createSsrLoaderPlugin.ts · start interceptor (hydrationState.name === state.name; channelAgrees(hydrationState.params, state.params); channelAgrees(hydrationState.search, state.search)), · channelAgrees; packages/ssr-utils/src/hydrateRouter.ts · hydrateRouter (ctx.hydrationState = parsed)",
   "evidence": "census-completeness/probe-hydration-channel-reads.ts: matching {committedData: from-server, payloadReads {path: 1, context: 1, name: 1, params: 1, search: 1}, paramsReads {id: 1}, searchReads {q: 1}, contextReads {data: 1}}; негативный контроль mismatching (params расходятся) {committedData: loaded-by-loader, payloadReads без search — короткое замыкание, paramsReads {id: 1}}."
  },
  {
   "what": "RouterError.setAdditionalFields·fields и RouterError.setErrorInstance·err — публичные методы того же класса (члены интерфейса RouterError), свёрнуты в entrySymbol двери RouterError.constructor·options без собственных id — нарушение собственного правила переписи «одна точка входа × одна позиция». setErrorInstance читает message/cause/stack по имени, cause сохраняется как лист по ссылке.",
   "where": "packages/core/src/RouterError.ts · setAdditionalFields (objectEntries + putField), · setErrorInstance (this.message = err.message; this.cause = err.cause; this.stack = err.stack ?? …)",
   "evidence": "census-completeness/probe-router-error-methods.ts: additional {reads {userId: 1, role: 1}, landed {userId: u1, role: admin}}; instance {reads {message: 1, cause: 1, stack: 1}, message: swapped, causeIsCallersLeafByReference: true}; censused-контроль ctor {reads {message: 1, segment: 1, path: 1, extra: 1}}."
  },
  {
   "what": "Алиасы точек входа без id (низкая важность, по собственному прецеденту переписи «PluginApi.X и RouterInternals.X — разные двери»): Router.constructor·routes|options|dependencies — класс Router экспортирован из index.ts, а createRouter — чистый pass-through new Router(routes, options, dependencies); PluginApi.forwardState·return и RouterInternals.forwardState·return — то же замыкание, что censused RouterInternals.port().resolveForward·return.",
   "where": "packages/core/src/index.ts (export { Router }); packages/core/src/createRouter.ts · createRouter; packages/core/src/api/getPluginApi.ts · forwardState (→ ctx.forwardState); packages/core/src/wiring/wireNamespaces.ts · createRouteResolver (resolveForward: (…) => ctx.forwardState(…))",
   "evidence": "Тождество по исходнику: createRouter.ts · createRouter возвращает new Router<Dependencies>(routes, options, dependencies) без иной работы; getPluginApi.ts · forwardState возвращает ctx.forwardState<P, S>(routeName, routeParams, routeSearch); createRouteResolver.resolveForward — алиас ctx.forwardState. Проб не требуется."
  }
 ],
 "notes": "ПРОВЕРЕНО МЕХАНИЧЕСКИ (списки и счёт). Знаменатель переписи: 190 id (сохранены в scratchpad, дублей 0). Мои пробы — W/benchmarks/audit-probes/membrane-globality-2026-09-05/census-completeness/ (12 файлов: probe-validator-identity, probe-pending-shell-cancel-error, probe-systemcommit-segments, probe-clone-rereads-options, probe-thenable-returns-emitter, probe-router-handout-factories, probe-shared-descriptor-reads, probe-hydration-channel-reads, probe-router-error-methods, probe-validate-route-export, probe-signal-handle, probe-matcher-buildpath-handout); все 12 отработали, решающие строки процитированы дословно; в каждой есть позитивный контроль (censused дверь / заведомо легальный вход) и доказательство достижения ветки (например committedData: from-server vs loaded-by-loader; cancelOutcome: CANCELLED). В каталоге проб уже существует чужой census-critic/ (файлы от 09-06) — НЕ читал, чтобы не смешивать выводы. Итог по числу: ~78 новых door-id при гранулярности переписи (26 валидатор-контейнеров + 1 лист + 5 снапшот-вложенных; 6 pending-шелл; 1 segments; 5 signal; 10 thenable; 7 router-хэндаутов фабрикам; 1 validateRoute (+children); 1 matcher.buildPath (+options); 8 app-слотов в options-хэндаутах; 3 shared; 3 hydrationState.*; 2 RouterError; 5 алиасов) — утверждение переписи «dropped пуст, 190» неполно прежде всего по семействам валидатор-колбэков, thenable-возвратов и PENDING-шелла. 1) ПУБЛИЧНЫЕ ЭКСПОРТЫ: index.ts — Router (класс → алиас конструктора, находка 13), createRouter ✓, getNavigator ✓, resolveForwardChain ✓ (сигнатура (startRoute, forwardMap, maxDepth) — других объектных параметров нет), RouterError (ctor ✓; setAdditionalFields/setErrorInstance — находка 12), events/constants/errorCodes/UNKNOWN_ROUTE — константы, не двери; api/index.ts — getPluginApi/getRoutesApi/getDependenciesApi/getLifecycleApi/cloneRouter ✓ (·router censused); validation.ts — getInternals ✓, validateRoute (находка 7), validateRouteType не экспортируется; utils.ts — copyFields/putField (примитивы: цель и источник — объекты ВЫЗЫВАЮЩЕГО, состояние ядра не затрагивают — исключены), freezeThrownError (морозит объект вызывающего на месте, ядро не хранит — исключён). ИНТЕРФЕЙСЫ, члены с объектным типом (все сверены с переписью): PluginApi 15 членов — makeState ✓, forwardState ✓ (+return алиас), matchPath (строка; return — объект ядра), navigateToState ✓ (+options.signal), setRootPath (строка), addEventListener (fn; return — находка 5), buildNavigationState ✓, getOptions ✓ (+app-слоты, находка 9), getTree ✓, addInterceptor (fn; возвраты интерцепторов ✓), extendRouter ✓, emitTransitionError ✓, claimContextNamespace (строка; write ✓), getRouteConfig ✓; RoutesApi 8 — add ✓ (routes+options), remove/has/clear (строки), update ✓ (+GuardFnFactory·router, находка 6), replace ✓, get ✓, subscribeChanges (event ✓; handler·return — проверен, отброшен обёрткой, НЕ дверь); DependenciesApi 7 — get/remove/has (ключи), getAll ✓, set ✓, setAll ✓, reset; LifecycleApi 4 — addActivateGuard/addDeactivateGuard (fn-лист; ·router — находка 6), remove* (строки); Router 22 членов + index signature — все параметры/возвраты сверены: isActiveRoute/buildPath/canNavigateTo/navigate/navigateToDefault/areStatesEqual/shouldUpdateNode/usePlugin/subscribe/subscribeLeave/getState/getPreviousState/start/navigateToNotFound ✓, находки: options.signal (4), subscribe·return (5), cancel/error toState (2), [key:string] ✓; RouterInternals 30 членов — все сверены; находки: forwardState·return алиас (13), routeGetStore().matcher.buildPath (8), getCloneState·return.options.* app-слоты (9), systemCommit·toState.transition.segments (3), hydrationState.name|params|search (11), addEventListener·cb·return (5); logger — объект ядра; getStateName/isTransitioning/isDisposed/getRootPath/setRootPath — скаляры; port() — resolveForward/defaultParams/defaultSearch/buildPath/queryNames/pathNames ✓, admitsUndeclaredQuery/report* — скаляр/функции; Plugin 8 хуков — toState/fromState/.context сверены (cancel/error toState — находка 2), возвраты — находка 5, opts — systemCommit·opts ✓; Route 11 полей + index — name/path строки, canActivate/canDeactivate (fn; ·router — находка 6), forwardTo (fn→string; ForwardToCallback·params ✓), children ✓, encodeParams/decodeParams (returns ✓; аргумент decoder — объект ядра после withoutUnsafeKey, аргумент encoder в buildPath — свежие spread-копии — исключены), defaultParams/defaultSearch ✓, custom ✓; RouteConfigUpdate 7 — forwardTo/canActivate/canDeactivate/codecs (fn), defaultParams/defaultSearch ✓; Options 12 — defaultRoute (→string), defaultParams/defaultSearch ✓ (+callback ✓), 5 скаляров, queryParams ✓ (вложенных контейнеров нет — 4 скалярных ключа по route-node-types.ts · QueryParamsOptions; перечитывается клоном — находка 9), logger ✓ (callback — fn-лист), limits ✓ (5 скаляров); NavigationOptions 7 — 6 boolean через adoptNavigationOptions ✓, signal — находка 4; RouterValidator 8 групп / 50 методов — call-sites: 63 формы ctx.validator?. + 6 getValidator()?. + 3 const validator (первый grep пропустил вторую форму — добран; она добавляет только скаляры/функции и validatePluginKeys, уже учтённый). 2) ВОЗВРАТЫ КОЛБЭКОВ: 14 *Callback/Fn-типов в types/router.ts + InterceptorFn×2 + ContextNamespaceClaim.write + shared/ssr (SsrLoaderFn ✓, SsrLoaderFnFactory — находка 6, SsrModeResolver → строка; его аргумент state — реле censused дверей next·return / LeaveState.nextRoute) + ssr-utils Deserialize ✓ + angular RequestDepsFactory ✓; LogCallback/DefaultRouteCallback/ForwardToCallback возвращают void/string — не двери; SubscribeFn/Plugin-хуки/addEventListener — находка 5; GuardFnFactory·return — fn-лист (ядро свойств не читает: RouteLifecycleNamespace хранит в Map<string, GuardFn>). 3) SHARED: 32 .ts по git ls-files shared, каждый классифицирован: связки с дверью ядра — getRouteFromEvent ✓, canSkipPopstateHistoryWrite ✓, createPluginBuildUrl (params|search ✓; opts — находка 10), createReplaceHistoryState (params|search ✓; options — находка 10), buildHref ✓, navigateWithHash ✓ (+extraOptions.signal — находка 4), resolveLinkTarget·to — находка 10, createSsrLoaderPlugin (loader·return ✓, hydrationState.context[ns] ✓, deferred keys ✓, name/params/search — находка 11, SsrLoaderFnFactory·router — находка 6), defer ✓, markStale ✓; исключены с причиной: popstate-handler deps (transitionOptions — литералы САМИХ плагинов: browser-plugin/hash-plugin/navigation-plugin · factory.ts, объект приложения туда не попадает), createOptionsValidator·opts (валидация опций плагина, состояние ядра не затрагивает), route-announcer/scroll-restore/scroll-spy/view-transitions/direction-tracker options (plugin-level; читают только State ядра; scroll-spy навигирует бэгами getState() — объекты ядра), deferRegistryClient (реестр создаёт собственный bootstrap плагина), createLoadersValidator (чистая валидация конфига плагина), url-*/history-api/safe-browser/ssr-fallback/defaults/detect/utils/types/index (строки, браузерные глобалы; history.state censused). 4) ХЭНДАУТЫ С ROUND-TRIP: getState (.context ✓; .transition.segments на арке systemCommit — находка 3; ядро само segments обратно не читает — grep .segments по core/src даёт только matchResult.segments движка; читают адаптеры), getOptions (находка 9: queryParams перечитан клоном — доказано счётчиком 1→2), getAll (копия, без round-trip — censused), getRouteConfig ✓, RoutesApi.get ✓ (round-trip через update), getTree ✓, matchPath·return / makeState·return / buildNavigationState·return — объекты ядра с интерьером-копиями, round-trip только через копирующие двери navigateToState — исключены. 5) ВЛОЖЕННЫЕ ПОЛЯ: children — рекурсия guardRouteStructure / snapshotRouteBatch / registerAllRouteHandlers / validateRoute ✓; defaultParams/defaultSearch/custom/codecs ✓ (+update ✓); queryParams/limits/logger — см. выше. ОГОВОРКИ: возвраты onStop/onTransitionCancel/onTransitionError и signal на navigateToDefault/navigateToState перечислены по символу (единственный диспетчер #invokeIsolated; единственная точка чтения opts.signal — executeNavigation), не прогонялись отдельно; счётчики countingProxy считают только get-трапы (ownKeys/descriptor-обходы не видны) — поэтому limits показывает 1 чтение при спреде, а objectKeys-перечисления в channelAgrees/ingest не считаются; вердикты P1–P4 по найденным дверям не выносились — задача была полнота переписи."
}