---
"@real-router/core": minor
---

Remove `ActivationFn` and `ActivationFnFactory` re-exports

**Breaking Change:** `ActivationFn` and `ActivationFnFactory` are no longer exported. Use `GuardFn` and `GuardFnFactory` instead.
