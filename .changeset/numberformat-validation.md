---
"@real-router/validation-plugin": minor
---

Add `numberFormat` validation support (#383)

Validate `queryParams.numberFormat` option — accepts `"none"` or `"auto"`, throws `TypeError` for invalid values.
