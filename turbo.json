{
  "$schema": "./node_modules/turbo/schema.json",
  "daemon": false,
  "globalPassThroughEnv": ["CI", "GITHUB_ACTIONS"],
  "globalDependencies": [
    "tsconfig.json",
    "prettier.config.*",
    "pnpm-lock.yaml",
    "tsup.base.*",
    "eslint.config.*",
    "vitest.config.*"
  ],
  "globalEnv": ["BENCH_ROUTER"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": [
        "src/**/*.{ts,tsx}",
        "tsup.config.*",
        "tsconfig.json",
        "package.json"
      ],
      "env": ["NODE_ENV"],
      "cache": true
    },
    "test": {
      "dependsOn": ["lint", "type-check"],
      "outputs": ["coverage/**"],
      "inputs": [
        "src/**/*.{ts,tsx}",
        "tests/**/*.{ts,tsx}",
        "vitest.config.*",
        "**/*.test.{ts,tsx}",
        "**/*.spec.{ts,tsx}"
      ],
      "env": ["NODE_ENV"],
      "passThroughEnv": ["CI"],
      "cache": true
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": [],
      "inputs": [
        "**/*.{ts,tsx,js,jsx}",
        "!dist/**",
        "!coverage/**",
        "eslint.config.*"
      ],
      "cache": true
    },
    "type-check": {
      "dependsOn": ["^build"],
      "outputs": [],
      "inputs": [
        "**/*.{ts,tsx}",
        "!dist/**",
        "tsconfig.json",
        "**/tsconfig.json"
      ],
      "cache": true
    },
    "lint:package": {
      "dependsOn": ["build"],
      "outputs": [],
      "inputs": ["dist/**", "package.json"],
      "cache": true
    },
    "lint:types": {
      "dependsOn": ["build"],
      "outputs": [],
      "inputs": ["dist/**", "package.json"],
      "cache": true
    },
    "build:dts": {
      "dependsOn": ["build"],
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**/*.d.ts", "dist/**/*.d.mts"],
      "cache": true
    }
  }
}
