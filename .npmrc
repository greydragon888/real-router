# pnpm 11: this file is for auth/registry settings only.
# All behavioral settings live in pnpm-workspace.yaml (pnpm no longer reads
# them from .npmrc or the package.json "pnpm" field). The project has no
# auth/registry overrides, so this file is intentionally otherwise empty.
