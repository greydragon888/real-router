{
  "name": "dom-utils",
  "version": "0.2.8",
  "private": true,
  "description": "Tests for shared/dom-utils — source files live in /shared/dom-utils (src is a symlink)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greydragon888/real-router.git"
  },
  "author": {
    "name": "Oleg Ivanov",
    "email": "greydragon888@gmail.com",
    "url": "https://github.com/greydragon888"
  },
  "license": "MIT",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint --cache --ext .ts src/ tests/ --fix --max-warnings 0",
    "test": "vitest run",
    "test:properties": "vitest run --config vitest.config.properties.mts"
  },
  "dependencies": {
    "@real-router/core": "workspace:^"
  },
  "devDependencies": {
    "jsdom": "28.1.0"
  }
}
