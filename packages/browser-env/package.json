{
  "name": "browser-env",
  "version": "0.2.9",
  "private": true,
  "description": "Tests for shared/browser-env — source files live in /shared/browser-env (src is a symlink)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greydragon888/real-router.git"
  },
  "author": {
    "name": "Oleg Ivanov",
    "email": "greydragon888@gmail.com",
    "url": "https://github.com/greydragon888"
  },
  "license": "MIT",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint --cache --ext .ts src/ tests/ --fix --max-warnings 0",
    "test": "vitest run",
    "test:properties": "vitest --config vitest.config.properties.mts --run"
  },
  "dependencies": {
    "@real-router/core": "workspace:^",
    "type-guards": "workspace:^"
  },
  "devDependencies": {
    "jsdom": "28.1.0"
  }
}
