<template>
  <div>
    <h1>About</h1>
    <p>
      The <code>createRouterPlugin</code> factory calls
      <code>app.provide()</code>
      internally — it resolves the same injection keys as
      <code>&lt;RouterProvider&gt;</code>.
    </p>
  </div>
</template>
