import type { JSX } from "react";

export function AdminUsers(): JSX.Element {
  return (
    <div>
      <h1>Admin — Users</h1>
      <p>
        Nested route: <code>admin.users</code>
      </p>
    </div>
  );
}
