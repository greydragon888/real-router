<div>
  <h1>Settings</h1>
  <p>A simple page with no nested routes — just a named snippet at the root level.</p>
  <div class="card">
    <p>
      In the root <code>RouteView</code>, this page is rendered by the
      <code>{"{#snippet settings()}"}</code> block.
    </p>
  </div>
</div>
