<div>
  <h1>Home</h1>
  <p>
    This example uses the <code>&lt;Lazy&gt;</code> component from
    <code>@real-router/svelte</code> for code-splitting.
  </p>
  <p>
    Dashboard and Analytics are loaded as separate chunks. The
    <code>Spinner</code> component is shown as fallback while the chunk loads.
  </p>
  <p>
    On second visit, chunks are cached — the spinner won't appear.
  </p>
</div>
