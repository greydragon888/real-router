<template>
  <div>
    <h1>Checkout</h1>
    <p>
      You navigated here using a <code>v-link</code> directive on a
      <code>&lt;button&gt;</code> or <code>&lt;a&gt;</code> element.
    </p>
    <div class="card">
      <p>
        The directive adds click/Enter handlers and cursor styling
        automatically.
      </p>
      <p>
        On non-interactive elements, it also adds <code>role="link"</code> and
        <code>tabindex="0"</code>.
      </p>
    </div>
  </div>
</template>
