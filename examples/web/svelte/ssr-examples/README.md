# Svelte SSR Examples

> Server-rendering with Real-Router and Svelte 5 across four delivery models — classical SSR, deferred-data SSR (often misnamed "streaming"), static site generation, and hybrid per-route mode.

Four standalone Vite apps. Each one is a real, e2e-tested dogfooding consumer of `@real-router/*` for a specific server-rendering shape; this document is the synthesis of what the router can and cannot do on Svelte 5, sourced exclusively from the four child READMEs.

Stack: `@real-router/core`, `@real-router/core/utils` (`createRequestScope`, `serializeRouterState`, `hydrateRouter`, `getStaticPaths`), `@real-router/svelte`, `@real-router/svelte/ssr` (`<ClientOnly>`, `<ServerOnly>`, `<Await>`, `<Streamed>`, `<HttpStatusCode>`, `<HttpStatusProvider>`, `useDeferred`, `createHttpStatusSink`), `@real-router/ssr-data-plugin` (`defer`, `invalidate`, `getSsrDataMode`), `@real-router/ssr-data-plugin/server` (`injectDeferredScripts`), `@real-router/ssr-data-plugin/errors`, `@real-router/browser-plugin`. SSR_FEATURE_GAPS_RU.md gaps #603–#611 closed as of 2026-05-15 — see "Status snapshot" at the bottom.

> **Terminology note up front.** `ssr-streaming/` here is **deferred-data SSR**, not HTTP streaming. Svelte 5 stable returns a single buffered HTML response; `{#await}` ships the pending branch and async resolution runs on the client after hydration. The child README documents this explicitly with a `node:http` reproducer (1 TCP frame, ~0 ms span) and contrasts it with React 19 / Solid (multiple frames, >400 ms span — true progressive HTTP flush). Do not generalise from sibling adapters.

| Subdir | Demonstrates |
|---|---|
| [`ssr/`](./ssr) | Classical per-request `createRequestScope(req, baseRouter, { currentUser })` (from `@real-router/core/utils` — clones base, wires `AbortController` to `req.on("close")`, auto-injects `abortSignal` into the deps map, exposes `dispose()`) → `usePlugin(ssrDataPluginFactory)` → `start(url)` → `await render(App, { props })` (Svelte's `render()` is `PromiseLike`, so the same call covers sync + async paths uniformly). `svelte.hydrate(App, { target, props })` claims SSR'd DOM. `<svelte:head>` content lands in `RenderOutput.head` and is spliced through `<!--ssr-head-->`. Auth + role gates via `canActivate` + DI; typed loader errors → 301/302/404/504; **render-time HTTP status via `<HttpStatusCode code={404}/>` + `<HttpStatusProvider {sink}>` + `createHttpStatusSink()` from `@real-router/svelte/ssr`** — `NotFound.svelte` declares its own status, `App.svelte` conditionally wraps the body in `<HttpStatusProvider>` via `{#if httpStatusSink}` (server-only branch), `entry-server` reads `sink.code ?? 200` after `render()` (replaces the prior `state.name === UNKNOWN_ROUTE ? 404 : 200` server-side check). Svelte 5 hydration tolerates `{#if}`-branch asymmetry, so `entry-client` doesn't pass the prop and skips the wrapper (verified by zero hydration warnings — contrast Vue/Solid which require symmetric mount). **SSR-boundary primitives `<ClientOnly>` + `<ServerOnly>` from `@real-router/svelte/ssr` (#604) dogfooded on the Home page** (`e2e/ssr-boundaries.spec.ts` asserts JS-disabled HTML emits the server branch and post-hydration DOM swaps both branches without `hydrat`/`mismatch` console warnings). Svelte 5 primitives demonstrated: `createSubscriber` (canonical bridge for `subscribe(callback)` APIs into the reactivity graph), `SvelteSet` (reactive collection wrapper). Production HTTP: sha256 strong **ETag** (16 base64url chars), per-route `Cache-Control`, `AbortController` auto-injected via `createRequestScope` so `getDep("abortSignal")` works inside loaders. 52 e2e total (50 in `ssr.spec.ts` + 2 in `ssr-boundaries.spec.ts`). |
| [`ssr-streaming/`](./ssr-streaming) | **Deferred-data SSR** via Svelte 5's `{#await}` blocks (NOT chunked HTTP). Server emits the pending fallback in one buffered body; `{:then}` resolution runs on the client after hydration. `<svelte:boundary>` for reactive/render-time errors with `@failed` snippet + `reset` + `onerror` (production observability). `<svelte:boundary pending>` + top-level `await` (gated behind `experimental.async: true` in Svelte 5.54.x). Per-request scope via `createRequestScope(req, baseRouter)` — same shape as `ssr/`. **Note on `defer()` (#610):** `@real-router/svelte/ssr` ships `useDeferred`/`<Await>`/`<Streamed>` and `@real-router/ssr-data-plugin` ships `defer({ critical, deferred })` with the `__rrDefer__("key", json)` inline-script settle wire — but this example deliberately does NOT dogfood them; Svelte 5 has no progressive HTTP-flush, late-resolving promises never stream, and native `{#await}` already covers the deferred-data shape (RSC-like: pending HTML from server, real content after client-side resolution). See `packages/ssr-data-plugin/CLAUDE.md` "Adapters that intentionally don't dogfood `defer()`" for the rationale. `use:` actions exercised end-to-end including the `update` lifecycle (param change → `action.update()`, no remount). 23 e2e tests (20 streaming scenarios + 3 post-hydration #596/#597 tests); Scenario 12 captures TCP-frame timing as falsifiable proof of "no progressive flush". |
| [`ssg/`](./ssg) | Build-time pre-rendering: `getStaticPaths(router, entries)` → `cloneRouter(base)` + `start` + `await render` per URL → write `dist/{url}/index.html` (`createRequestScope` is not used at build time — no per-request lifecycle to wire). Nested SSG (8 paths total: 1 home + 1 list + 3 profiles + 3 posts). Per-page meta with 9 tags (title, description, canonical, og:type/title/description/url/image, twitter:card); profile pages use `og:type=profile`, posts use `article`; `<svelte:head>` content additionally injected via `<!--ssr-head-->`. `404.html` + `sitemap.xml` + filesystem-layout assertions + overfetch protection + sitemap↔disk consistency. Dual-mode mount: explicit `if (rootElement.firstElementChild) hydrate(...) else mount(...)` (Svelte 5 has no `mount({ hydrate: true })`). 25 e2e tests. |
| [`ssr-mixed/`](./ssr-mixed) | Per-route SSR mode (#597) — four routes × four config shapes on one `entry-server.ts`: `home` short-form (full), `admin.dashboard` `{ ssr: false }` (client-only), `users.profile` `{ ssr: "data-only", loader }`, `docs.detail` `{ ssr: (state) => …, loader }` (function-form). `entry-server.ts` uses `createRequestScope(req, baseRouter)` and reads `getSsrDataMode(state)` to branch between `render(App, { props: { router } })` from `svelte/server` (full, returns `{ head, body }` so `<!--ssr-head-->` placeholder is needed) or emits `<div data-ssr-shell data-ssr-mode="…">` shell. **Adapter quirk:** `useRoute()` must be called inside `RouterProvider` context, so the example splits into `App.svelte` (provider) + `AppView.svelte` (consumer with route switch) — different from React/Vue/Solid where the provider and consumer can share one component. Additionally dogfoods **#605 per-namespace CSR revalidation**: Home page imports `invalidate(router, "data")` from `@real-router/ssr-data-plugin` + `router.navigate(name, params, search, { reload: true })`; the `home` loader observes `SsrLoaderContext.signal` (`(_target, ctx?) => { ... ctx?.signal.addEventListener("abort", …) }`) to demonstrate the cancel-safe loader contract. 7 e2e tests total (4 mode-shape + 3 invalidate: happy-path Refresh, rapid double-click cancels first nav, abort counter increments when nav AbortSignal fires). Composes with #596: hydration scratchpad reuse for `full`/`data-only`; `client-only` skips loader unconditionally. |

## Capabilities

What Real-Router actually delivers on Svelte 5 across these four shapes, grouped by concern. Each item is sourced from one or more child READMEs.

**Per-request isolation.** Runtime examples (`ssr/`, `ssr-streaming/`, `ssr-mixed/`) use `createRequestScope(req, baseRouter, deps?)` from `@real-router/core/utils` — the four-step boilerplate replacement that produces an isolated router clone, attaches an `AbortSignal` to the request's lifetime (Node `IncomingMessage#"close"` event), injects `abortSignal` into the deps map, and exposes `dispose()` for `finally`. `ssg/` calls `cloneRouter(base)` directly — at build time there's no per-request lifecycle to wire. All scopes are disposed in `finally` so a typed-error short-circuit, a deferred-data exit, or a client disconnect never leaks state. The DI seam carries `currentUser` (auth/role guards) alongside the auto-injected `abortSignal` (cancellation); `entry-server/_auth.ts` and `entry-client.ts` mirror cookie parsing through shared `_known-users.ts` so post-hydration `canActivate(getDep("currentUser"))` outcomes match the SSR pass.

**Single data-loading plugin shape, with a Svelte SSG divergence.** `@real-router/ssr-data-plugin` writes plain JSON-shaped data into `state.context.data` for all four pipelines and ships a typed-errors subpath (`@real-router/ssr-data-plugin/errors`) with `LoaderNotFound`/`LoaderRedirect`/`LoaderTimeout` + `withTimeout`. Loader signature is `(router, getDep) => (params, ctx?: { signal }) => data` — the optional `ctx.signal` (#605) is the navigation's `AbortSignal` для cancel-safe writes (dogfooded в `ssr-mixed/`). **Note:** Svelte SSG loaders deliberately do **not** opt into the typed-error path (return `user: undefined` for unknown ids; pages handle the undefined branch in snippet templates). The `ssr/`, `ssr-streaming/`, and `ssr-mixed/` examples do use typed errors.

**State serialization + hydration.** `serializeRouterState()` is XSS-safe; `hydrateRouter(router, ssrState)` deposits the parsed state into a one-shot scratchpad on `RouterInternals.hydrationState` and calls `router.start(state.path)` once on the client; `ssr-data-plugin`'s start interceptor reads the scratchpad and reuses the server-resolved `state.context.data` directly, skipping the loader call (#596).

**`hydrate` ≠ `mount` in Svelte 5.** Both live as separate top-level exports of `svelte`. `hydrate(App, { target, props })` claims existing DOM; `mount(App, { target, props })` mounts fresh. There is **no** `mount({ hydrate: true })` option in Svelte 5 — that is the deprecated Svelte 4 compat surface via `asClassComponent`. SSG branches explicitly: `rootElement.firstElementChild ? hydrate(...) : mount(...)` — same `entry-client.ts` works for SSG-rendered files and Vite dev fallbacks.

**`render()` is uniformly sync + async.** `RenderOutput = SyncRenderOutput & PromiseLike<SyncRenderOutput>` — the same call returns a sync object you can read directly *and* a thenable you can `await`. Using `await render(App, …)` covers both sync and async (top-level `await`, `<svelte:boundary pending>`) paths uniformly.

**`<svelte:head>` declarative head injection.** Components contribute to `<head>` via `<svelte:head>` blocks; `render()` collects them into the `head` field. The server splices it through `<!--ssr-head-->`. SSG additionally renders 9 tags including OpenGraph + Twitter Card (per-id absolute canonical, not the parent URL). This is the Svelte alternative to manual `meta.ts` injection — and it composes with explicit `<!--ssr-meta-->` placeholders too (SSG uses both: `<svelte:head>` content AND a meta-resolver block).

**Snippet-based `<RouteView>`.** Svelte 5 named snippets matching route segments. `notFound` and `self` are reserved slot names (see `RESERVED_SLOT_NAMES`). Inside a parent `<RouteView nodeName="users">`, the parent's *own* leaf renders into `self` while nested children render into snippets named after them.

**Loader-driven HTTP error model (where opted in).** Loader errors map to HTTP responses via `entry-server.ts` dispatch: `CANNOT_ACTIVATE` → 302, `LOADER_REDIRECT` → 301/302, `LOADER_NOT_FOUND` → 404 text/plain, `LOADER_TIMEOUT` → 504 text/plain. The deferred-data pipeline (`ssr-streaming/`) catches `LoaderNotFound` **before** rendering — typed errors keep the contract identical to classical SSR. `withTimeout()` (#598) composes the deadline with the per-request `abortSignal` via `options.upstreamSignal`: a single `{ signal }` aborts on whichever fires first, and `fetch(..., { signal })` propagates the abort to the network layer. The `slow` loader demonstrates the composed pattern end-to-end.

**Render-time HTTP status (`<HttpStatusCode code={N}/>`).** Complementary to the loader-driven model: when the status is decided by the *rendered component* (e.g. a glob `*` route → `NotFound.svelte`) rather than by a loader exception, mount `<HttpStatusCode>` from `@real-router/svelte/ssr`. The component reads the optional `getContext(HTTP_STATUS_KEY)` sink and writes `code` once at component init; `entry-server.ts` reads `sink.code ?? 200` after `render()` to set the response status. Renders nothing (no DOM); silent no-op on the client when no provider is mounted. **Svelte-specific: hydration tolerates `{#if}`-branch asymmetry** — `App.svelte`'s `{#if httpStatusSink}` wrapper is server-only (`entry-client.ts` doesn't pass the prop), and zero hydration warnings fire. This is unique to Svelte 5; Vue/Solid require symmetric provider mounting on both sides. 3 dedicated dogfood e2e tests verify the contract. Streaming/SSG examples don't dogfood this — `ssg/` builds 404 at build-time via `404.html`; `ssr-streaming/` short-circuits unknown ids via `LoaderNotFound`.

**Svelte 5 reactive primitives applicable to SSR.**

- **`createSubscriber` (Svelte 5)** — canonical way to integrate `subscribe(callback)` APIs (websocket, IntersectionObserver, MediaQueryList) into the reactivity graph; the SSR ships the initial value, the client lazily sets up the subscription on first read inside an effect/template. Analogous to React's `useSyncExternalStore` and Solid's `createResource`. Demonstrated by `clock.svelte.ts` on the home page of `ssr/`.
- **`SvelteSet` / `SvelteMap` / `SvelteDate` / `SvelteURL` / `SvelteURLSearchParams`** — reactive collection wrappers; mutations notify the graph without immutable replacement. Demonstrated by `UsersList.svelte` selection state. **Client-only by example design** — never serialized to SSR (selection starts empty after hydration).
- **`<svelte:boundary>` for reactive/render-time errors** — separate from `{#await}`'s `{:catch}`. The `@failed` snippet renders the fallback with a `reset` callback to recover; `onerror` callback fires for production observability. Demonstrated in `ssr-streaming/` Scenarios 15/17.
- **`use:` actions** — server runtime skips invocations entirely, so action bodies can reference DOM-only APIs (e.g. `IntersectionObserver` in `use:trackView`) without crashing render. Mount/update/destroy lifecycle exercised end-to-end (Scenario 20: `action.update({ productId: "999" })` fires on the existing observer instance, no remount).
- **`$state.raw(value)`** — Svelte 5 escape hatch for large data objects replaced wholesale; skips the deep proxy. Mentioned in `ssr-streaming/README.md` as available; not used in the example because in-memory fixtures are small.

**Composables and context.** Composables (`useRoute`, `useNavigator`, etc.) return `{ current: T }` getters — read inside reactive contexts (template, `$derived`, `$effect`). `getContext` must be called during component init; calling it inside `$effect`, event handlers, or async callbacks throws.

**Per-route SSR mode (#597).** `@real-router/ssr-data-plugin` accepts a per-route `{ ssr?, loader? }` config that branches the server pipeline by `SsrMode`: `"full"` (run loader + render), `"data-only"` (run loader, ship JSON only, shell HTML), `"client-only"` (skip loader, ship shell, application fetches client-side). `ssr: false` aliases `"client-only"`; `ssr: true` aliases `"full"`. Function form `(state) => SsrMode` resolves per-navigation. The plugin publishes the resolved mode to `state.context.ssrDataMode`; `entry-server.ts` reads it via `getSsrDataMode(state)` to branch between `render(App, { props: { router } })` from `svelte/server` (full) and a shell `<div data-ssr-shell data-ssr-mode="…">` placeholder. The `ssr-mixed/` example demonstrates all four config shapes on one server. Note that because Svelte composables (`useRoute`) require the `RouterProvider` context, the example splits the tree into `App.svelte` (provider) + `AppView.svelte` (consumer with route switch). Composes with #596: hydration scratchpad reuse applies when mode allows the loader; `client-only` skips the loader unconditionally regardless of scratchpad contents (mode wins).

**Per-namespace CSR revalidation (#605).** `@real-router/ssr-data-plugin` exports `invalidate(router, namespace)` — a peek-then-clear-after-write primitive that marks a context namespace stale. Dogfooded in `ssr-mixed/`'s Home page: `invalidate(router, "data")` + `router.navigate(name, params, search, { reload: true })` re-runs the loader on the same route. The plugin's `stabilizeState` was updated so `{ reload: true }` is not short-circuited as a same-state no-op. Loaders observe the navigation's `AbortSignal` via `SsrLoaderContext.signal` — rapid double-clicks abort the first nav, its `signal.addEventListener("abort", …)` fires, and the cancel-safe write semantics keep the stale flag set so the second nav's leave handler can consume it. Verified by 3 dedicated e2e tests in `ssr-mixed/`.

**SSR-boundary primitives (#604).** `@real-router/svelte/ssr` exports `<ClientOnly>` (renders fallback on the server, children after hydration via Svelte 5 `$state` rune) and `<ServerOnly>` (renders children on the server, fallback after hydration — symmetric inverse) — paired primitives for declarative branch-by-environment markup. Dogfooded in `ssr/`'s Home page; verified by 2 e2e tests asserting JS-disabled HTML shows the server branch and post-hydration DOM swaps both branches without `hydrat`/`mismatch` console warnings. Cross-adapter (React/Preact/Vue/Solid/Svelte/Angular).

**`defer()` API is available but not dogfooded here.** `@real-router/svelte/ssr` ships `useDeferred`/`<Await>`/`<Streamed>` (built on Svelte 5 `{#await}` block under the hood; `useDeferred` returns `Promise<T>` for direct use with native `{#await}`) and `@real-router/ssr-data-plugin` ships `defer({ critical, deferred })` (#610) with the `__rrDefer__("key", json)` inline-script settle wire format emitted by `injectDeferredScripts` (from `@real-router/ssr-data-plugin/server`). The Svelte `ssr-streaming/` example deliberately uses native `{#await}` directly — Svelte 5 has no progressive HTTP-flush, late-resolving promises never stream, and native `{#await}` already covers the RSC-like deferred-data shape (pending HTML from server, real content after client-side resolution). The cross-adapter `defer()` contract remains available for cross-framework consistency.

**Build-time path enumeration.** `getStaticPaths(router)` walks the route tree and returns **leaf** paths only. `ssg-build.ts` derives intermediate parent URLs from leaves (e.g. `/users/:id` from `/users/:id/posts`) and adds the `/users` list page manually. Result: 8 pre-rendered URLs.

**Per-route HTTP semantics, per pipeline.** The HTTP envelope diverges sharply across the four pipelines:

| Pipeline | `Cache-Control` | `ETag` | `AbortController` |
|---|---|---|---|
| `ssr/` (renderToString*) | per-route, `must-revalidate` | strong sha256 (16 base64url chars) | `createRequestScope(req, baseRouter, { currentUser })` auto-injects `abortSignal` into deps; loader pulls via `getDep("abortSignal")` (manual `as unknown as` cast — deps map is `Record<string, unknown>`) |
| `ssr-streaming/` (deferred-data) | **none — out of scope by design** | **none** | `createRequestScope` auto-injects `abortSignal`, but example doesn't wire it through (out of scope) |
| `ssg/` (build-time) | **none** — `ssgServe()` is redirect-only | none | not applicable |
| `ssr-mixed/` (per-route mode) | not wired (mode-shape demo) | not wired | `createRequestScope(req, baseRouter)` auto-injects `abortSignal`; `home` loader reads it via `SsrLoaderContext.signal` for #605 cancel-safety |

The `ssr-streaming/` envelope is documented as "out of scope" — the example focuses on the deferred-data + boundary contract. The SSG envelope mirrors the same divergence as Solid SSG (`Cache-Control` + weak ETag are documented follow-ups; the pattern lives in Vue/React `ssg/` configs).

## Limitations

Each item is categorised: **(framework-side)** — Svelte 5 / vite-plugin-svelte / runtime constraint not under Real-Router's control; **(design choice)** — deliberate Real-Router (or plugin) omission with documented rationale, often closeable by user-side composition; **(example design choice)** — per-example scope narrowing or simplification, not a library/adapter constraint.

- **(framework-side) Svelte 5 stable does NOT implement chunked HTTP streaming with out-of-order placeholders.** `svelte/server.render` returns a single buffered HTML response. `{#await}` ships the **pending** branch in the body; resolution runs on the client. No `Transfer-Encoding: chunked`, no `<template id="…">` OOO chunks, no selective hydration — `hydrate()` claims the full tree atomically. See "Why no `ssr-rsc/`?" below.
- **(framework-side) `<svelte:boundary pending>` does NOT block server-side render in Svelte 5.54.** The boundary's `pending` snippet ships to the wire just like `{#await}` would; async resolution happens on the client. Documented as honestly testable (Scenario 16) — if a future Svelte release flips to "server waits before flush" semantics, the test fails noisily by design.
- **(framework-side) Top-level `await` is gated behind `experimental.async: true`** in Svelte 5.54.x — configured in both `vite.config.ts` (compiler hot path) and `svelte.config.js` (svelte-check). Expected to graduate to stable in a future Svelte minor.
- **(framework-side) Don't override Vite `resolve.conditions`.** Replacing the default list drops the implicit `"browser"` condition for the client build, and `import { hydrate } from "svelte"` resolves to `index-server.js` where `hydrate()` throws `lifecycle_function_unavailable`. Use `dedupe: ["svelte"]` only — see the inline comment in `ssr/vite.config.ts`.
- **(framework-side) `<Lazy>` is not for SSR data.** It uses `$effect` to start its loader, and `$effect` does not fire on the server — so SSR/SSG output renders **only** the fallback. For SSR-critical data, use `state.context.data` (via `ssr-data-plugin`) or a top-level `await` in `<script>`.
- **(framework-side) `withTimeout()` cancellation is cooperative.** When the deadline elapses, the loader's `{ signal }` aborts *before* the race rejects with `LoaderTimeout`; loaders that thread `signal` into their I/O actually cancel the underlying work. Loaders that don't propagate the signal still run to completion in the background. The `slow` loader in `ssr/` composes the deadline with `options.upstreamSignal` (client-disconnect from per-request DI).
- **(example design choice) `ssr-streaming/` does not dogfood `defer()` + `<Await>` + `<Streamed>`** even though the adapter ships them (`@real-router/svelte/ssr` exports `useDeferred`/`<Await>`/`<Streamed>`; `@real-router/ssr-data-plugin` exports `defer()` and `injectDeferredScripts`). Svelte 5 has no progressive HTTP-flush — `render()` produces one buffered body, `{#await}` ships its pending branch and async resolution runs client-side after hydration — so `__rrDefer__("key", json)` settle scripts have no streaming benefit on Svelte. Native `{#await}` already covers the RSC-like deferred-data shape (pending HTML from server, real content after client-side resolution). The cross-adapter `defer()` contract remains available for cross-framework consistency or for application code wanting unified loader-return shape. See `packages/ssr-data-plugin/CLAUDE.md` "Adapters that intentionally don't dogfood `defer()`".
- **(design choice) No router-specific streaming/deferred-data API beyond `defer()` + `<Await>` + `<Streamed>`.** Streaming behaviour comes from Svelte 5 native `{#await}` + `<svelte:boundary>`; the router's role is identical to non-streaming SSR (per-request `createRequestScope`, `start(url)`, plugin-driven critical data). "Standalone Svelte SSR with deferred-data semantics through native primitives, without SvelteKit framework lock-in" — `ssr-streaming/README.md` Library Philosophy.
- **(design choice) `ssr-streaming/` ships no production HTTP semantics.** `AbortController`, `ETag`, `Cache-Control`, HEAD-handler, and per-route `<svelte:head>` are intentionally out of scope — the example focuses on the deferred-data + boundary contract. The sibling `ssr/` ships the full prod envelope; this example stays narrow on purpose.
- **(design choice) `ssr-data-plugin` (boot-path) and `lifecycle-plugin onNavigate` (CSR navigation) split the lifecycle by phase, on purpose.** `ssr-data-plugin` intercepts `router.start()` — runs once during SSR; on the client, `hydrateRouter(router, ssrState)` deposits the parsed state into a one-shot scratchpad and the plugin reuses the server-resolved `state.context.data` directly (#596) instead of calling the loader again. CSR navigations via `<Link>` do NOT re-run its loader; that is `lifecycle-plugin.onNavigate`'s job — explicitly documented as the canonical client-side path in the Svelte child READMEs. Single-responsibility per plugin keeps SSG-only build scripts and pure-CSR SPAs from carrying interception code they don't use.
- **(example design choice) Svelte SSG loaders are tolerant, not strict.** Loaders are user code; `ssr-data-plugin` does not dictate throw-vs-return for unknown ids. The Svelte SSG `loaders.ts` returns `user: undefined` (pages handle the undefined branch via snippet templates); Vue/React/Angular SSG throw `LoaderNotFound`. Both contracts are valid — Svelte chose the lenient one (matching Solid). The fail-fast guard in `ssg-build.ts` is wired but dormant; opting into the strict path is a one-file change.
- **(example design choice) Svelte SSG ships no `Cache-Control` / weak-ETag override.** `ssgServe()` is a per-example local Vite plugin (not a `@real-router/*` export); the Vue/React `ssg/` versions add a `getCachePolicy` lookup + `res.writeHead` interception, the Svelte/Solid versions are redirect-only. Adding cache policies is application-level work; pattern lives in `examples/web/vue/ssr-examples/ssg/vite.config.ts`.
- **(example design choice) `404`/`504` response bodies are plain text, not the SSR-rendered NotFound page.** Rendering a rich 404 would require a second `render()` pass against a different URL — the demo deliberately keeps it simple. Application code that wants a styled 404 can do the second pass; nothing in `@real-router/*` blocks it. (Note: SSG additionally serves `404.html` with the correct HTTP status only if the host web server is configured for it — nginx `try_files`, Cloudflare/Netlify `_redirects` — that part is deployment-side.)
- **(framework-side) `abortSignal` requires a manual cast in loader DI.** `createRequestScope` deps map is `Record<string, unknown>` (same as `cloneRouter` under the hood), so loaders consuming `getDep("abortSignal")` need a manual `as unknown as (key) => AbortSignal | undefined` cast. Pattern documented in `ssr/src/router/loaders.ts`.

### Why no `ssr-rsc/`?

Svelte does not implement React Server Components. The `ssr-streaming/README.md` comparison table characterises Svelte 5's network model as "lazy hydration only — single response, server shell + client data" — there is no Flight-style server-component-tree wire format, and no chunked progressive flush in stable Svelte 5. The `{#await}` block + `<svelte:boundary>` form the closest native equivalent for async-aware SSR. If you need RSC, use the React adapter — see [`../../react/ssr-examples/ssr-rsc/`](../../react/ssr-examples/ssr-rsc).

## Svelte-unique aspects

What's specific to the Svelte 5 adapter, expressed as Real-Router integration points rather than Svelte theory:

- **The "streaming" terminology trap.** `ssr-streaming/` is deferred-data SSR, not HTTP streaming — falsifiably so (Scenario 12: 1 TCP frame, ~0 ms span via `node:http`). Real-Router is identical to the classical `ssr/` example here; the only thing that differs is what each `{#await}` block ships in the body. Adapter-shopping readers expecting React-19 / Solid behaviour will be surprised — the README's terminology disclaimer is load-bearing.
- **`<svelte:boundary>` is a separate axis from `{#await}` `{:catch}`.** `{:catch}` covers async loader rejections; `<svelte:boundary>` covers reactive/render-time errors with `@failed` snippet + `reset` + `onerror`. Application code combines them; the router contributes nothing extra.
- **Snippet-based RouteView matches Svelte's idioms.** `{#snippet name()}{/snippet}` blocks named after route segments — `notFound` and `self` reserved. Nested `<RouteView nodeName="users">` inside `UserProfile.svelte` shows the local-nesting pattern (no top-level `users.profile.posts` snippet in `App.svelte`).
- **Two render-output channels for `<head>`.** `RenderOutput.head` (auto-collected from `<svelte:head>` blocks) flows through `<!--ssr-head-->`; an explicit meta-resolver block flows through `<!--ssr-meta-->` (SSG uses both simultaneously). Either alone is fine; combined they let `<svelte:head>` cover per-component contributions and the meta-resolver cover route-level SEO.

## Empirical findings

Findings that the four READMEs flag as non-obvious, empirically-verified, or footgun-class — distilled here so adapter-shopping readers don't have to re-discover them.

1. **Svelte 5 "streaming" SSR is, empirically, NOT streaming over HTTP.** `ssr-streaming/` Scenario 12 uses `node:http` to count frames: body lands in 1 TCP frame, ~0 ms span. The symmetric test in Solid `ssr-streaming/` (Scenario 17) measures multiple frames spanning >400 ms — true OOO over chunked HTTP. Svelte's `<50 ms` assertion inverts the assertion direction. Falsifiable proof of "no progressive flush", documented as a regression guard for any future Svelte release that ships actual HTTP streaming.

2. **`<svelte:boundary pending>` does NOT block server-side render in Svelte 5.54.** Pending snippet ships to the wire; async resolution happens on the client. Scenario 16 pins this behaviour — if a future Svelte release flips to "server waits before flush", the test fails honestly.

3. **Don't override Vite `resolve.conditions` — drops the implicit `"browser"` condition for the client build.** Symptom: `import { hydrate } from "svelte"` resolves to `index-server.js`, `hydrate()` throws `lifecycle_function_unavailable`. Use `dedupe: ["svelte"]` only. Critical non-obvious detail in `ssr/README.md` "Svelte-Specific Gotchas".

4. **`render()` is `PromiseLike` even for sync components — `await render(...)` works uniformly.** `RenderOutput = SyncRenderOutput & PromiseLike<SyncRenderOutput>`. Calling `await render(App, …)` covers both top-level-await components and pure-sync components without branching. Documented as the canonical entry-point shape.

5. **Svelte SSG diverges from Vue/React/Angular by example design.** Loaders are tolerant (return `undefined`); `ssgServe()` does not override `Cache-Control` or attach a weak ETag. Same divergence as Solid SSG — explicit follow-ups, not bugs.

6. **`404.html` SEO status depends on the host web server.** The build emits `dist/404.html`, but Vite preview / `sirv` may serve it with HTTP 200 unless explicit `try_files`-equivalent is configured. Real production needs nginx `try_files`, Cloudflare/Netlify `_redirects`, etc.

7. **`invalidate(router, namespace)` + `{ reload: true }` survives rapid double-clicks (#605).** Naive `invalidate + navigate` is unsafe: a late-resolving loader from nav A can race nav B and overwrite fresh data with stale. The plugin's peek-then-clear-after-write semantics keeps the stale flag set while the loader is in flight; if the navigation aborts, the late loader sees `signal.aborted` and skips the write — the flag stays set for the next nav to consume. Verified in `ssr-mixed/` by the `aborts` counter ticking up on rapid double-clicks while `fetchedAt` still lands on a fresh timestamp.

8. **Svelte 5 hydration tolerates `{#if}`-branch asymmetry (Svelte-distinct from Vue/Solid).** `<HttpStatusProvider>` is mounted only on the server (via `{#if httpStatusSink}` in `App.svelte` — `entry-client.ts` doesn't pass the prop), and post-hydration walker emits zero warnings. Vue requires symmetric `<HttpStatusProvider>` mount (fragment markers `<!--[-->`/`<!--]-->`); Solid requires symmetric mount (`data-hk` per-component markers). Svelte's tolerance lets the application omit the throwaway client-side provider — a small but observable adapter difference.

9. **`@real-router/svelte/ssr` is a separate package entry (#609), not a root export.** All SSR-only components (`<ClientOnly>`, `<ServerOnly>`, `<Await>`, `<Streamed>`, `<HttpStatusCode>`, `<HttpStatusProvider>`) and composables (`useDeferred`, `createHttpStatusSink`) ship under the `/ssr` subpath — symmetric with React/Preact/Vue/Solid/Angular. CSR-only consumers don't pay for the SSR surface; SSR consumers import from one place. Built by svelte-package as a separate dist entry.

## Status snapshot (2026-05-15)

Closure of `.claude/SSR_FEATURE_GAPS_RU.md` items against the Svelte adapter:

| Issue | Description | Status |
|---|---|---|
| #603 | `createRequestScope` helper in `@real-router/core/utils` | ✅ shipped — used by `ssr/`, `ssr-streaming/`, `ssr-mixed/` |
| #604 | `<ClientOnly>` + `<ServerOnly>` SSR-boundary primitives | ✅ shipped — `@real-router/svelte/ssr` (built on Svelte 5 `$state` rune); dogfooded in `ssr/`, 2 e2e |
| #605 | `invalidate(router, namespace)` + cancel-safe loaders | ✅ shipped — `@real-router/ssr-data-plugin`; dogfooded in `ssr-mixed/`, 3 e2e |
| #606 | Opt-in `serialize`/`deserialize` for devalue/superjson | ✅ shipped in core — Svelte examples ship default JSON shape (no non-JSON types in fixtures) |
| #607 | (postponed — see SSR_FEATURE_GAPS_RU.md) | ⛔ postponed |
| #608 | (postponed — see SSR_FEATURE_GAPS_RU.md) | ⛔ postponed |
| #609 | `/ssr` subpath split for adapter SSR surface | ✅ shipped — `@real-router/svelte/ssr` is a distinct entry; symmetric with React/Preact/Vue/Solid/Angular |
| #610 | `defer({ critical, deferred })` formal API + `__rrDefer__` wire | ✅ shipped — `@real-router/ssr-data-plugin` exports `defer` + `injectDeferredScripts`; **not dogfooded in `ssr-streaming/`** (Svelte 5 has no progressive HTTP-flush; native `{#await}` covers deferred-data shape) |
| #611 | `<HttpStatusCode>` + `<HttpStatusProvider>` + `createHttpStatusSink()` | ✅ shipped — `@real-router/svelte/ssr`; dogfooded in `ssr/`, 3 e2e |

## See Also

- [`@real-router/svelte`](../../../../packages/svelte) — adapter package (`RouterProvider`, `RouteView`, `Link`, `useRoute`, snippets)
- [`@real-router/svelte/ssr`](../../../../packages/svelte/src/ssr.ts) — SSR-feature subpath (`ClientOnly`, `ServerOnly`, `Await`, `Streamed`, `HttpStatusCode`, `HttpStatusProvider`, `useDeferred`, `createHttpStatusSink`)
- [`@real-router/core/utils`](../../../../packages/core/src/utils) — `createRequestScope`, `serializeRouterState`, `hydrateRouter`, `getStaticPaths`
- [`@real-router/ssr-data-plugin`](../../../../packages/ssr-data-plugin) — JSON-shaped per-route data loading (used by all four pipelines; SSG opts out of typed errors; exports `defer`, `invalidate`, `getSsrDataMode`)
- [`@real-router/ssr-data-plugin/server`](../../../../packages/ssr-data-plugin/src/server.ts) — `injectDeferredScripts` for `__rrDefer__` settle wire
- [`@real-router/ssr-data-plugin/errors`](../../../../packages/ssr-data-plugin/src/errors.ts) — typed loader errors (`LoaderRedirect`, `LoaderNotFound`, `LoaderTimeout`, `withTimeout`)
- [`@real-router/browser-plugin`](../../../../packages/browser-plugin) — client-side URL sync after hydration (`ssr/`, `ssg/`, `ssr-mixed/`; `ssr-streaming/` deliberately omits)
- [`.claude/SSR_FEATURE_GAPS_RU.md`](../../../../.claude/SSR_FEATURE_GAPS_RU.md) — gap-implementation roadmap tracking issues #603–#611
- [React SSR examples](../../react/ssr-examples) — full feature parity reference, plus `ssr-rsc/` for React Server Components
- [Solid SSR examples](../../solid/ssr-examples) — counterpart with true OOO HTTP streaming + selective hydration (the streaming-divergence comparison)
- [Vue SSR examples](../../vue/ssr-examples) — counterpart with chunked HTTP streaming + blocking SSR Suspense (contrast)
- Svelte docs: [`render`](https://svelte.dev/docs/svelte/svelte-server), [`hydrate`](https://svelte.dev/docs/svelte/imperative-component-api), [`mount`](https://svelte.dev/docs/svelte/imperative-component-api), [`<svelte:head>`](https://svelte.dev/docs/svelte/svelte-head), [`<svelte:boundary>`](https://svelte.dev/docs/svelte/svelte-boundary), [`{#await}`](https://svelte.dev/docs/svelte/await)
