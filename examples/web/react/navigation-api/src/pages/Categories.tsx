import type { JSX } from "react";

export function Categories(): JSX.Element {
  return (
    <section>
      <h1>Categories</h1>
      <p>
        Flat categories placeholder — this example keeps categories simple on
        purpose, because exclusive-extension scenarios already cover parameter
        access via <code>products.product</code>.
      </p>
    </section>
  );
}
