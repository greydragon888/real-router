# React Hash Routing Example

Demonstrates hash-based routing for static hosting (GitHub Pages, S3).

## What it covers

- `hashPluginFactory({ hashPrefix: "!" })` — URLs use `#!/path` prefix
- Deep linking — reload preserves the hash, landing on the correct route
- Zero server configuration — works on any static file server

## Run

```bash
pnpm install
pnpm dev
```
